# Repository

## Primary goal

The Repository package serves as a reusable business logic layer for other services. For instance, both mission and planet services depend on it to handle resource updates before taking any action.